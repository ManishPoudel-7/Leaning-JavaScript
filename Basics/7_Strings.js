const name = "Manish"
const repoCount =  5

console.log(name + " repos are : " + repoCount) // This is outdated works fine so better to do it in the following way

console.log(`Hello my name is ${name.toUpperCase()} and the count of my repos are ${repoCount}`)   // This is better as we can perform more actions ot the inout as you can see

const gameName = new String("Indo")

console.log(gameName[0])
console.log(gameName.__proto__)

console.log(gameName.length)
console.log(gameName.toLowerCase())

console.log(gameName.indexOf('I'))

console.log(gameName.substring(3))  // Jitna number doge utna hta dega aage se

 