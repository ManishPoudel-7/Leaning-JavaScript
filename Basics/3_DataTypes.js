"use strict";   //Used to treat all the following code as newer version

// alert(1+3)   cant be used because we are using node js instead of any browser

let name = "Manish"
let age = 22
let votingright = true;
let degree;
let job = "null"

/*
number =  -2^53 to 2^53-1
bigint
string = ""
boolean = true/false
null = standalone value
undefined = value not assigned yet
symbol = unique (mostly used in react)
*/

console.log(typeof "Manish")
console.log(typeof name)

console.log(typeof null)            //Object
console.log(typeof undefined)       // undefined